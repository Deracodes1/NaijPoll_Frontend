import { StatusbadgePipe } from './statusbadge-pipe';

describe('StatusbadgePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusbadgePipe();
    expect(pipe).toBeTruthy();
  });
});
